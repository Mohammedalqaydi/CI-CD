name: Readme checker

on:
  pull_request:
    branches:
      - main

jobs:
  test_pull:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      run: echo "Hello, World!"

